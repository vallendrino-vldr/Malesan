"use client";

/**
 * Malesan is now permanently Dark Mode only.
 * This component is kept as a null export to avoid breaking any legacy references.
 */
export function ThemeToggle({
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  variant = "switch",
}: {
  variant?: "switch" | "chip";
} = {}) {
  return null;
}
