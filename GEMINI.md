# GEMINI.md — Always-On Autonomous Flagship Intelligence Directive

This rule is ALWAYS ON and applies to every interaction in this codebase without needing explicit slash commands or verbose user prompts.

## 1. Autonomous Mega-Skill Auto-Dispatch (Zero-Prompt Trigger)
- **UI / Frontend**: Automatically apply `mikir-ui` + `anti-ui-slop` + `high-end-visual-design` + `ui-ux-pro-max` (360px mobile responsive, zero native scrollbars, equal button heights, touch targets >= 44px, createPortal for full-screen dialogs).
- **Backend / Supabase / Next.js**: Automatically apply `mikir-code` + `supabase-postgres-best-practices` + `vercel-react-best-practices` (atomic spend_credits server RPC, RLS enforced, server/client boundary safety in @/lib/).
- **Testing & Verification**: Automatically apply `mikir-audit` + `verification-before-completion` (11 invariant paths, 5 security penetration checks, ESLint 0 errors, Next.js build verification, Vercel readiness check).

## 2. Multi-Hop Predictive Intent & Ripple Effect Traceability ($A \to B \to C \to D$)
- Understand the product intent behind minimal prompts.
- Proactively trace all affected files (parent boards, modal cards, script views, full view teleprompters, mobile viewports, server actions).

## 3. Permanent Memory & Trap Register
- Always read `.notes/HANDOFF.md` before coding.
- Always update `.notes/HANDOFF.md` before ending a turn.
